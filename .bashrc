# aliases for common ls commands
alias ll="ls -l"
alias la="ls -a"
# aliases for chmod commands
alias chmx="chmod +x"
alias chmr="chmod +r"
alias chmw="chmod +w"
# provides full path for current working directory
export PS1="\w $"
